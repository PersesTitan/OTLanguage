package bin.check;

public class VariableType {
    public enum Origin {
        Boolean, Character, Double, Float, Integer, Long, String
    }

    public enum List {
        Boolean, Character, Double, Float, Integer, Long, String
    }

    public enum Map {
        Boolean, Character, Double, Float, Integer, Long, String
    }

    public enum Set {
        Boolean, Character, Double, Float, Integer, Long, String
    }
}
